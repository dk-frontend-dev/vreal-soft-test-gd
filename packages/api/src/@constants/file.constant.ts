import {join} from "path";

export const ONE_MB = 1024 * 1024;

export const ALLOWED_FILE_TYPES = ['png', 'jpeg', 'jpg', 'svg', 'mp4', 'mov'];

export const filesDir = join(__dirname, '..', 'public');
